const initialState = {

}

function activeReducer( state = initialState, action){
    switch(action.type){
        case "ACTIVE_COMPONENT":
          // wait shouldn't the React Router Deal With This?  
    }
}